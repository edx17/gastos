node_modules
dist
dist-ssr
*.local
.env
.env.*
!.env.example
# También sin el punto: es fácil guardarlo así sin querer y publicar las claves.
env
env.*
*.env
.DS_Store
coverage
.vite
*.log
supabase/.temp
# El bundle de migraciones se genera; no va al repo ni, sobre todo, dentro de
# supabase/migrations (Supabase intentaría aplicarlo como una migración más).
crocante-migraciones.sql
crocantemigraciones.sql
# Caché de compilación de TypeScript: se regenera en cada build.
tsconfig.tsbuildinfo
